node_modules/
dist/
target/
src-tauri/target/
# Bundled heed sidecar — fetched at build time by scripts/fetch-heed.sh
src-tauri/binaries/
.DS_Store
*.log
.env
.env.*
*.pem
*.key
credentials.json

# Internal files
docs/
scripts/*
# ...except the build-required heed sidecar fetch script (CI runs it).
!scripts/fetch-heed.sh
tests/
CLAUDE.md
.claude/
src-tauri/gen/schemas/
